<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hemraj.technorio_posts">

    <!--permission for internet-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PostActivity"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TagActivity"
            android:label="@string/tag_list" />

        <activity
            android:name=".activity.POST.Post1Tag"
            android:label="@string/tag_list" />

        <activity
            android:name=".activity.POST.Post2Tag"
            android:label="@string/tag_list" />

        <activity
            android:name=".activity.POST.Post3Tag"
            android:label="@string/tag_list" />

        <activity
            android:name=".activity.POST.Post4Tag"
            android:label="@string/tag_list" />

        <activity
            android:name=".activity.TAG.Tag1Post"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TAG.Tag3Post"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TAG.Tag4Post"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TAG.Tag5Post"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TAG.Tag6Post"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TAG.Tag7Post"
            android:label="@string/post_list" />

        <activity
            android:name=".activity.TAG.Tag8Post"
            android:label="@string/post_list" />

    </application>

</manifest>